interface:
  display_name: "Gmail検索"
  short_description: "Gmail検索をgog CLIで安全に実行するスキル"
  default_prompt: "Use $gmail-search-gog to search Gmail with `gog` and summarize the matching results in Japanese."

policy:
  allow_implicit_invocation: true
